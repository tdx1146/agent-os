# 轻如烟 · 工具清单
> 所有可用工具一览（2026-07-01）

## 一、OpenClaw 原生工具

| 工具 | 用途 | 调用方式 |
|------|------|----------|
| `read` | 读取文件内容 | 直接调用 |
| `write` | 创建/覆盖文件 | 直接调用 |
| `edit` | 精确替换文件内容 | 直接调用 |
| `apply_patch` | 多文件补丁 | 直接调用 |
| `exec` | 执行 shell 命令 | 直接调用 |
| `process` | 管理后台进程 | 配合 exec |
| `web_search` | 搜索引擎搜索 | 直接调用 |
| `web_fetch` | 网页内容抓取 | 直接调用 |
| `image` | 图片分析 | 直接调用 |
| `pdf` | PDF 分析 | 直接调用 |
| `memory_get` | 精确读取 memory/ 文件 | 直接调用 |
| `memory_search` | 语义搜索记忆 | 直接调用 |
| `nodes` | 管理配对设备 | 直接调用 |
| `tts` | 文字转语音 | 直接调用 |
| `create_goal` | 创建目标 | 直接调用 |
| `get_goal` | 获取当前目标 | 直接调用 |
| `update_goal` | 更新目标状态 | 直接调用 |
| `sessions_yield` | 等待子代理结果 | spawn 子代理后用 |
| `skill_workshop` | 创建/管理技能 | 直接调用 |

## 二、dandan MCP 工具（18 个）

**服务器**: `node /vol1/@apphome/trim.openclaw/data/workspace/dandan-mcp-server.mjs`
**端口**: 23621（内部）

| 工具 | 用途 |
|------|------|
| `sandglass__openalex_search` | 学术论文搜索（OpenAlex，免费无需 key） |
| `sandglass__read_backlog` | 读取轻如烟待办 backlog.md |
| `sandglass__sandglass_chart` | 情绪熵 ASCII 可视化图表 |
| `sandglass__sandglass_dream` | 幽灵决策推理 |
| `sandglass__sandglass_echo` | 当前回音折风向 |
| `sandglass__sandglass_export` | 导出沙漏数据 |
| `sandglass__sandglass_import` | 导入外部对话（ChatGPT/Claude） |
| `sandglass__sandglass_migrate` | 一键导出全部记忆为 tar.gz |
| `sandglass__sandglass_offset` | 当前偏移率 |
| `sandglass__sandglass_persona` | 当前阶段画像 |
| `sandglass__sandglass_ping` | 健康检查 |
| `sandglass__sandglass_recent` | 最近 N 条记忆 |
| `sandglass__sandglass_search` | 关键词搜索记忆 |
| `sandglass__sandglass_semantic` | 语义搜索记忆 |
| `sandglass__sandglass_soul_export` | 导出灵魂差分 |
| `sandglass__sandglass_soul_merge` | 合并外部灵魂差分 |
| `sandglass__sandglass_tasks` | 待办事项列表 |
| `sandglass__sandglass_thread` / `_add` / `_graph` / `_weave` | 织线知识图谱 |

## 三、Sandglass MCP 工具

**服务器**: `python3 /vol2/1000/AI专用/所有自动化/轻如烟/sandglass_source/sandglass_mcp.py`
**数据路径**: `/vol2/1000/AI专用/所有自动化/轻如烟/sandglass`
**环境变量**: `NEXSANDBASE_HOME`

> 注意：sandglass 的 MCP 工具通过 dandan MCP 包装暴露（工具名前缀 `sandglass__`）。
> 以上 18 个工具实际由 dandan-mcp-server.mjs 统一提供，sandglass 本身不直接暴露 MCP。

## 四、补充工具（由技能/配置启用）

| 工具来源 | 说明 |
|----------|------|
| `bundle-mcp` | 通过 sandbox.tools.alsoAllow 启用 |

## 验证 MCP 是否加载

```bash
# 检查 MCP 服务器进程
ps aux | grep -E 'dandan-mcp-server|sandglass_mcp' | grep -v grep

# 验证 18 个工具已加载 — 检查 openclaw.log 或 /tmp/mcp-tools-count
# 如果工具 < 18，从 facts.dict.md SYS-07 恢复
```
