# 事件拓扑报告 — 2026-08-04

> 生成方式：`python3 src/topology.py`（iso-sand，Phase 5 治理时点）
> 数据来源：`deploy/event_schema.yaml`（18 事件注册）+ `src/handlers.py`（7 handler）+ `deploy/event_rules.yaml`（3 legacy 规则）
> 关联文档：`ARCHITECTURE-FINAL.md`（权威架构）、`deploy/event_schema.yaml`（契约）

---

## 一、拓扑总表

| 事件类型 | 生产者 | 消费者/订阅方 | 状态 |
|---------|--------|--------------|------|
| `task_complete` | core/task_scheduler | handler `audit.task_complete`（result=OK）；handler `lms.feed`；rule-task-complete（legacy → 玄鉴审计） | active |
| `anomaly` | core 多源（task_scheduler / event_consumer / 胶水） | handler `alert.anomaly`（result=FAIL）；rule-anomaly-escalate（legacy → alerts.log） | active |
| `audit_result` | xuanjian（玄鉴审计完成，D2 延后未实际接入） | handler `archive.audit_result`（result=OK）；rule-audit-archive（legacy → log_archive） | active |
| `milestone` | core/丰碑 | handler `lms.feed` | active |
| `consumer_action` | core/event_consumer | —（operation_log 自记录，无人订阅） | active（孤儿事件⚠️） |
| `interfaces.store` | interfaces（胶水/总线侧） | handler `interfaces.store`；handler `lms.feed` | active |
| `interfaces.recall` | interfaces（胶水/总线侧） | handler `interfaces.recall` | active |
| `lms.plastified` | lms | —（软参考信号，无人订阅属预期） | active（孤儿事件⚠️） |
| `lms.self_ref` | lms | —（软参考信号，默认关闭发布） | active（孤儿事件⚠️） |
| `lms.dream_complete` | lms | —（软参考信号） | active（孤儿事件⚠️） |
| `lms.feed` | lms | —（该事件类型是 handler 名，LMS 侧发布的是 store/task_complete/milestone 触发喂入） | active（孤儿事件⚠️） |
| `xuanjian.pipe` | xuanjian | handler `xuanjian.pipe`（占位 no-op，D2 延后） | active（占位） |
| `sandglass.heartbeat` | sandglass（心跳任务 `bus_heartbeat` 每 5 分钟） | — | reserved_phase2（仅注册未接线，预期） |
| `scheduler.tick` | scheduler | — | reserved_phase2（仅注册未接线，预期） |
| `scheduler.task_done` | scheduler | — | reserved_phase2（仅注册未接线，预期） |
| `ai.decided` | ai | — | reserved_phase2（仅注册未接线，预期） |
| `ai.action_done` | ai | — | reserved_phase2（仅注册未接线，预期） |
| `xuanjian.audit_result` | xuanjian | — | reserved_phase2（仅注册未接线，预期） |

## 二、handler 注册表（7 个）

| handler 签名 | 订阅 event_types | result 过滤 | 说明 |
|-------------|-----------------|------------|------|
| `audit.task_complete` | task_complete | OK | 调丰碑 audit.py --event（shell=False 数组传参） |
| `alert.anomaly` | anomaly | FAIL | 写丰碑 alerts.log（JSON 行） |
| `archive.audit_result` | audit_result | OK | 调丰碑 archive.py --event |
| `xuanjian.pipe` | xuanjian.pipe | None | 玄鉴预留占位（D2 延后，no-op 仅记录） |
| `interfaces.store` | interfaces.store | None | POST glue_server:19000 /store（聚合写入沙漏+LMS+向量） |
| `interfaces.recall` | interfaces.recall | None | POST glue_server:19000 /recall，结果写回 operation_log |
| `lms.feed` | interfaces.store / task_complete / milestone | None | POST LMS:8190 /feed（塑形喂入，限流 ≥1s，fail-open） |

## 三、环状订阅检测

- 订阅边：core→alert、core→rules、core→archive、interfaces→lms、core→lms、core→audit
- 自环（INFO，非有害）：interfaces→interfaces（recall/store 自观察）、xuanjian→xuanjian（pipe 自观察）
- **✅ 无长度 ≥2 的订阅环**（设计红线 R2 未触发）

## 四、检查项（拓扑工具输出）

- ⚠️ WARN 孤儿事件（active 但无人消费）：`consumer_action`、`lms.plastified`、`lms.self_ref`、`lms.dream_complete`、`lms.feed`
  - 说明：consumer_action 是消费者自记录（operation_log），非订阅语义；lms.* 是软参考信号（订阅方可忽略），孤儿属设计预期 —— 不处理
- ℹ️ INFO 预留命名空间（reserved_phase2，预期）：sandglass.heartbeat / scheduler.tick / scheduler.task_done / ai.decided / ai.action_done / xuanjian.audit_result

## 五、结论

总线拓扑健康：核心链路（调度器→任务完成→审计/记忆喂入；胶水 store/recall；LMS 双向反馈）全部接通；
无环、无异常注册。5 个 active 孤儿事件均为"自记录/软参考"语义，符合哑管道设计。
