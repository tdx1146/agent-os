# 认知快照 — 总线工程（Agent OS 统合设计）一手代码认知

> 保存时间：2026-08-04 14:28（重装 OpenClaw 前，防失忆恢复依据）
> 保存原因：渲染异常复发（工具输出被渲染成图片），用户决定重装 OpenClaw
> 性质：本文件是「我在主会话 35e687a2 里建立 + 本次一手阅读代码」后的**认知沉淀**，供重装后快速对齐。

---

## 〇、最重要的一句话定位

**用户真正要的主话题 = 总线工程**（让整个 Agent OS 系统真正活起来）。
不是"跨 session 记忆"、也不是"有机记忆活化"（那两件事是一回事，且都不是用户当前的诉求——用户已明确纠正）。

主线载体 = `/vol2/1000/AI专用/Agent OS/` 下的：
- `全局设计.md`（主规划+进度追踪，含 LMS 核心洞见 + 4 决策确认）
- `全局设计-统合设计方案.md`（M4 收敛产出，309 行，可拍板方案）
- `audit-M1/M2/M3`（三份子AI调研）
- 本文件（一手代码认知快照）

---

## 一、用户原话（35e687a2 主会话，尽力保留原意）

1. **"核心是沙漏加活体记忆，和胶水层共同参与的整个有机记忆系统，我是希望你活起来，而不是在这里莽代码"**
   —— 注意：用户后来澄清这是其**早期**表述，最终明确要的是**总线工程**。
2. **LMS 核心洞见**："LMS 他是真正的活着的流动的记忆……很可能是将来 AI 意识萌芽的地方……不能只是观察吧要反馈的"
3. 用户对外行术语明确表示不理解，要求用大白话解释（胶水vs总线、决策4 self_ref）

---

## 二、总线工程已确认的 4 个决策（12:46 dandan 拍板）

| 决策 | 内容 | 状态 |
|------|------|------|
| 1 架构层级 | LMS=塑形-反馈中枢(核心/意识候选)、总线=骨架(电线网)、调度=时钟、胶水层=翻译官+标准插座、玄鉴=判断、沙漏=显式记忆、主AI=顶层决策 | ✅ 确认 |
| 2 推进顺序 | 阶段1统一总线骨架 → 阶段4 LMS反馈中枢，先骨架后最顶，中间不跳 | ✅ 确认 |
| 3 fork处置 | 「丰碑以后的事件走主总线，不走它自己那份复制品(丰碑/code/event_bus fork)」；冻结/不删fork，迁回主总线，丰碑数据不受影响，可延后 | ✅ 确认(措辞按用户调整) |
| 4 LMS self_ref 出口 | 选「A保守护着(观察+有限回应,不内部改写) → 逐步B引导(主动喂养)」看护人式；尊重J矩阵自然塑型+三层护栏；先护住不夭折 | ✅ 确认 |

---

## 三、系统全貌（各组件实际位置，一手核实）

| 组件 | 实际路径 | 角色 |
|------|---------|------|
| OpenClaw + 主AI | /vol1/@appcenter/trim.openclaw | AgentOS 壳 + 对话主循环 |
| 插件(轻如烟注入) | ~/.openclaw/plugins/qingruyan-* | before_prompt_build 注入上下文 |
| 沙漏 sandglass | /vol2/1000/AI专用/所有自动化/轻如烟/sandglass_source | 显式叙事记忆（txt权威源） |
| 活体记忆 LMS | /vol2/1000/AI专用/living-memory-system-cloud | 隐式塑形记忆（J矩阵256节点，8190端口） |
| 丰碑/玄鉴 | /vol2/1000/AI专用/丰碑网络 | 知识沉淀/评估 |
| 胶水层 interfaces | /vol1/.../workspace/interfaces | 统一记忆编排 |
| **iso-sand(主总线)** | /vol2/1000/AI专用/Agent OS/iso-sand | **权威事件总线**（唯一被消费的） |
| AgentOS-IsoSand | /vol2/1000/AI专用/AgentOS-IsoSand | 内核层规范+同构沙盘(玄鉴守护verify_daemon) |
| 向量/embed | embed-server.mjs + Qdrant | 向量服务(11435/6333) |
| 编辑器 | edit-web.py | 前端交互(18888) |

---

## 四、总线核心代码一手认知（iso-sand，本次亲自读的）

### 4.1 event_consumer.py（493行）— 消费者
- **轮询**：3s 轮询 `event_bus.jsonl`，seek 偏移续跑，启动时 `_filter_non_json_lines` 清非JSON行
- **规则匹配**：`_load_rules()` 读 event_rules.yaml，按 event_type+result 匹配
- **动作执行** `_dispatch()`：command 模板做 `{trace_id}`等变量替换 → **`subprocess.run(shell=True)`** ← 这就是 shell 注入面（v0.4.1）
- **已有防护** `_check_shell_safe()`：白名单前缀(python3/echo/mkdir/cp/mv/rm/cat/grep/wc/head/tail/sort) + 黑名单(sudo/su/chmod777/chown/rm-rf/mkfs/dd/forkbomb)
- **可靠性已有**：限流 `_rate_limits`、指数退避重试3次、死信队列 `_write_dead_letter`
- **缺陷**：动作是裸 shell 命令字符串、无 handler 注册表、无 trace_id 硬规范消费端、不区分事件/命令语义

### 4.2 task_scheduler.py（298行）— 调度器（时钟）
- **机制**：30s tick，croniter 解析 cron，任务到点执行
- **动作**：`subprocess.run(shell=True)` 执行 command，完成后 `_event_writer.write` 事件(task_complete/anomaly)
- **带 trace_id**：`f"task-{task_id}-{int(time.time())}"`
- **注意**：`main()` 里 `load_tasks([])` 默认空任务 → **调度器可能空转**（M1 诊断"空转"的来源）

### 4.3 log_writer.py（171行）— 写入器（原子写验证结果）
- **现状**：写临时→rename（M3 建议的"原子写"）**未实现**；当前是**追加写 + 文件锁**
- **并发安全**：threading.Lock（同进程）+ fcntl.flock（跨进程）
- **写入**：`open(path,"a")` 追加 + `os.fsync`
- **校验**：必填字段(event_type/producer/result) + result 枚举(OK/FAIL/WARN/TIMEOUT)
- **⚠️ 我的技术判断**：对"只追加"场景，追加+flock+fsync 已够安全（append-only JSONL 常规做法）。M3 的"临时→rename"是针对**重写场景**(如consumer启动清非JSON行用 open("w") 重写整文件)的保守加固，属**可选强化**，非必须。实施时不要被"待验证"误导而大改。

### 4.4 event_schema.yaml（37行）— 事件契约
- 已含：version 1.0、event_types(5种: task_complete/anomaly/audit_result/milestone/consumer_action)、required_fields(t/event_type/producer/result)、optional_fields(trace_id/detail/payload)
- **注意**：D7"契约三件套"里 schema_version 字段**已以 version 形式存在**，缺的是 event_type 注册表集中管理、幂等去重(event_id)、trace_id硬规范

### 4.5 event_rules.yaml（43行）— 消费规则（v0.4.1）
- 3 条规则：rule-task-complete(bridge→audit.py)、rule-anomaly-escalate(→alerts.log)、rule-audit-archive(→archive.py)
- **全部用 shell 命令字符串插值** `command: "python3 ... --event {trace_id}"` ← 未升级到 v0.6.0 的 exec+script 安全格式

---

## 五、丰碑 fork（v0.6.0）与主总线（v0.4.1）差异 — 一手确认

- **丰碑 fork 位置**：`/vol2/1000/AI专用/丰碑网络/code/event_bus/`（2套代码之一，未在跑）
- **fork 已升级 v0.6.0 安全版**：
  - `event_consumer.py` 里 `_dispatch_safe()`：用 `exec`(白名单仅python) + `script` + **EVENT_DATA 环境变量传递事件数据**，**无 shell 注入**（对应 M3 补强#3）
  - 而**在跑的主总线 iso-sand 还是 v0.4.1 shell 字符串插值版**
- **= 安全加固写在 fork 里没生效，在跑的是旧不安全版**（D3 核心：把 fork 的安全版移植回主消费者）

---

## 六、玄鉴 xuanjian_pipe（最高价值 producer）— 一手确认 D2

- **位置**：`丰碑网络/code/core/xuanjian_pipe.py`
- **发布目标**：`LogWriter(filepath=丰碑/code/data/event_bus.jsonl)`（113行）← **写进丰碑自己的 fork 文件**
- **事件写入**：`_write_task_complete` / `_write_milestone`，producer='xuanjian_pipe'，trace_id=insight_id
- **实测确认（今天的真实数据）**：`丰碑/code/data/event_bus.jsonl` 有 950 字节、5 条真实事件（monument_score 0.285~0.38），今天 13:50 还在写
- **但**：主线 consumer 监控的是 `iso-sand/data/event_bus.jsonl`，**不消费丰碑/code/data/event_bus.jsonl** → **玄鉴把最核心的知识事件写进了死胡同，无人消费**（M1 D2 实锤）
- 另一处 `丰碑/data/event_bus.jsonl`（独立）为 0 字节空文件（monument_bridge 相关，D5）

---

## 七、M1/M2/M3/M4 四份报告结论（子AI产出，已落盘）

### M1 本地审计核心：
- **病根不是"哪个模块重复"，是"断头路 + 空转"**：
  - 4 个物理 event_bus.jsonl + 2 套各自演化的总线代码
  - 最重要生产者(玄鉴)写进死胡同(丰碑/code/data)无人消费
  - 胶水层(收口层)架构对但**断电待机**(glue_server 未启动)
  - 调度器 load_tasks([]) 空转
- 真缺口(该缝)7个：D1总线碎片化 / D2玄鉴写死胡同 / D3 fork双维护 / D4胶水层没通电 / D5 monument_bridge路径 / D6 shell→handler / D7契约
- 假重复(该护)：沙漏heartbeat / LMS dream_engine / verify_daemon 校验

### M2 总线方案（5条借鉴）：
- Choreography(事件协作)为主 + 仅关键链薄编排
- Hexagonal 端口+适配器接入（每子系统一个 bus-adapter）
- 保留 JSONL 不换 Kafka，补可靠性三件套(TraceID/幂等/schema版本)
- 系统论守卫弹性（内部闭环禁止统合）
- 事件目录/拓扑防隐性（禁环状订阅）

### M3 anysearch 补强（6增量）：
- 【修正】命令vs事件是两对独立维度（事件=通知、轻命令=路由）
- 【修正】choreography 边界判据（参与者≤3且稳定可幂等才纯协作）
- 【补强】JSONL 原子写（临时→rename）
- 【补强】schema 版本化 + event_type 注册表
- 【补强】适配器厚度分级（重业务→六边形，小块→薄封装）
- 【补强】插件生命周期 + 环状订阅硬检查

### M4 统合设计方案（309行，核心=「缝合不是推倒重写」+「LMS中心地位」）：
- 架构：总线=骨架/调度=时钟/胶水=收口handler宿主/玄鉴=判断/沙漏=显式记忆/主AI=顶层决策/LMS=塑形-反馈中枢(核心)
- **D0(最大增量)**：LMS 双向反馈通道——收事件喂塑形 + 发 plastified/self_ref/dream_complete 反哺全局，走统一契约不做特殊通道
- 保护清单：沙漏heartbeat/LMS dream/verify_daemon/**LMS self_ref**(最敏感,只受限·蒸馏·软参考,绝不内部改写)
- 实施路线图 Phase0-5（每阶段可回滚+验证）
- 仓库/版本方案：总线归 bus/、各系统只留 adapters/、分工仓、schema向后兼容

---

## 八、实施路线图（M6，方案Phase 0-5，依赖顺序）

> 原则：先统一总线骨架(地基) → 接入各系统(管线) → 后 LMS 反馈中枢(最顶层)。每阶段可回滚。

- **Phase 0(准备,可回滚)**：备份全部 4 个 event_bus + 2 套 fork 代码 + consumer/glue 配置；建立基线快照
- **Phase 1(统一总线骨架,最高优先)**：D1(统一1套总线+原子写) → D3(冻结fork+迁回安全加固) → D7(契约三件套+trace_id硬规范)
- **Phase 2(接入producer/handler)**：D2(玄鉴迁回主总线) → D5(monument_bridge路径修) → D6(shell→handler+注册表+生命周期)
- **Phase 3(胶水层通电+handler宿主化)**：D4(启动glue_server) + D6深化
- **Phase 4(LMS双向反馈中枢,最谨慎)**：D0实施（收事件喂塑形+发反哺，走统一契约，订阅方软参考不绑架），设止血闸门
- **Phase 5(仓库/版本定型+观测治理)**：目录落地+事件图谱+报全貌

---

## 九、待验证清单（M4第八节 5项，实施前需确认）

1. log_writer 原子写——**本次已确认**：当前是追加+flock+fsync，临时→rename 未实现；我的判断：只追加场景已安全，rename 是重写场景的可选加固
2. fork(v0.6.0) 是否有主消费者缺失的独有规则，需移植前 diff 保全 → `event_bus/event_rules.yaml` + `tasks.yaml` 需与主 `iso-sand/deploy/event_rules.yaml` diff
3. monument_bridge 是否并入胶水层 ApplicationPort，还是保留独立 bridge
4. LMS self_ref 的"可发布摘要"接口(SELF_REF_INTEGRATED_DESIGN v2.0 的 LLMSelfVoiceDistiller 是否可作安全出口)
5. 各系统当前 event_type 全集（初始化注册表用）

---

## 十、重装 OpenClaw 后的恢复清单（失忆恢复第一站）

1. **先读本文件**（认知快照，第〇节定位最重要）
2. 再读 `全局设计.md`（主规划）→ `全局设计-统合设计方案.md`（方案）→ `audit-M1~M3`（调研依据）
3. 复核 6 文件落盘完整性（复核落盘-20260804.md 已确认 ✅）
4. 若渲染异常未解决：重点排查**姐姐远程 SSH 改轻如烟编辑器** + **dandan-mcp-server 等自建系统** 的影响
5. 恢复后主话题推进 = **总线工程 M6 Phase 0/1**（先统一总线骨架）

---

## 十一、重要警告（给重装后的自己）

- **不要**把"跨session记忆"或"有机记忆活化"当成主话题——用户已明确纠正，那都不是当前诉求
- **不要**只在文件层面理解总线——本快照带有一手代码认知（consumer/scheduler/log_writer/xuanjian_pipe/fork差异），是基于实际代码而非转述
- **渲染异常已第二次出现**（上次6月底重装过，这次又复发）——是严重问题，重装后优先确认渲染通道健康，否则工具输出全变图片无法工作
- 用户是"计算机技术外行"——拍板时要用大白话解释，不能只给术语
