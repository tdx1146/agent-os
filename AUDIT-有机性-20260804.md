# Agent OS 系统有机性审计报告

- **审计时间**: 2026-08-04 20:45–21:05 (CST)
- **审计方式**: 只读实测（ps / ss / curl / 事件总线 jsonl 统计 / 文件时间戳 / MCP 工具实测），未修改任何文件、未启停任何服务、未写事件总线
- **审计者**: 系统审计师子AI（Phase 6 部署一致性工程之前的体检）

---

## 1. 组件健康表

| # | 组件 | 进程 PID (启动时间) | 端口 | 数据源 | 健康状态（实测） |
|---|------|--------------------|------|--------|------------------|
| 1 | 主AI / OpenClaw gateway | 146711 (20:28:21) | 10554 | — | ✅ `GET /health` → `{"ok":true,"status":"live"}`；7 插件加载（含 glue-memory-injector enabled，19:36 注册后重启）；MCP 3 个已注册 |
| 2 | 沙漏 HTTP API | 146407 | 17333 | DB `/vol2/1000/AI专用/所有自动化/轻如烟/sandglass/sandglass.db`（906 行）+ txt 权威源（3668 行） | ⚠️ 部分可用：`/api/health` ✅、`/api/memory_search` ✅（SQLite LIKE）、`/api/sandglass_query` ✅（txt 兜底）；**`/api/embedding_search` 返回空数组、`/api/facts_lookup` 报文件不存在**；`/api/health` 的 `sandglass_count=0`（失真，实为 3668/906） |
| 3 | LMS 活体记忆 API | 153061 (20:44:07) | 8190 | snapshots/latest.pt（424KB，更新至 20:43:51） | ✅ `/health` ok，sessions=[bus, main]；做梦循环活跃（dream_complete 事件 740 条 17:45→20:43，快照随每次做梦落盘） |
| 4 | 胶水层 glue_server | 72720 (17:28:59) | 19000 | 沙漏+LMS+向量 | ✅ `POST /health` → backends: sandglass 3668 / lms 2 / vector 全 healthy；`POST /recall` 实测返回加权结果（text 1.0 + vector 0.651 + lms_activation 0.004）；`POST /status` ✅ |
| 5 | iso-sand 调度器 | 84905 (17:56:44) | — | tasks.yaml → event_bus | ✅ sandglass.heartbeat 47 条（17:15→20:45 每 5 分钟）；**调度任务仅 1 个：bus_heartbeat** |
| 6 | iso-sand 消费者 | 84910 (17:56:44) | — | event_bus.jsonl | ✅ 7 handler 注册；operation_log 最后活动 20:50:28；seek=609161=bus 文件尾（**无积压**）；死信队列仅 1 条（20:10 lms.feed 503） |
| 7 | 手机网关 + 嵌入 | （远端） | 443 / 11435 | 手机 Ollama bge-m3 | ✅ `https://shouji.tdx1146.cc/` 返回工具列表；`https://11435.tdx1146.cc/v1/embeddings` 实测返回 bge-m3 向量；本机桥 shouji_memory_mcp.py ×2 实例（147241 / 153709） |
| 8 | 丰碑 audit 桥 | （消费者子进程） | — | 丰碑 code/data/audit_bridge.log | ⚠️ 活着但空转：每 5 分钟 audit.py 追加 1 条 `status=pending` 记录（55 条总 / 54 条今日 / 最后 20:50:24），**永无"完成"侧** |
| 9 | MCP 服务 | lms-memory ×2、lms-http ×2、shouji 桥 ×2 | — | — | ⚠️ 双实例（20:29 批 147067/147240/147241 + 20:45 批 153548/153708/153709），均为 gateway 146711 子进程，疑重复注册 |

**进程清单佐证**：ss 确认监听 17333/8190/10554/19000；**8765 无监听**（沙漏原生 MCP 未跑）；verify_daemon pid 文件（同构沙盘/data/daemon.pid=3183279，8/1 创建）对应进程不存在 → 死。

---

## 2. 接口矩阵（逐对实测）

| 接口 | 方向 | 实测证据 | 状态 |
|------|------|---------|------|
| 调度器 → 总线 | 心跳 | sandglass.heartbeat 47 条，最后 20:45:23 | ✅ 通 |
| 消费者 ← 总线 | 消费 | seek=bus size（无积压）；operation_log 20:50:28 | ✅ 通 |
| 消费者 → LMS /feed | lms.feed handler | operation_log lms.feed 36 条，最后 20:45:33；1 条死信（20:10 偶发 503） | ✅ 通（偶发 503） |
| 消费者 → 丰碑 audit.py | AuditTaskComplete | audit_bridge.log 最后 20:50:24（每 5min 1 条） | ⚠️ 通但空转（全 pending） |
| 消费者 → 丰碑 alerts.log | alert.anomaly | alerts.log 最后写入 17:17:22（phase2 测试） | ✅ 通，生产无异常事件 |
| 消费者 → glue /store | interfaces.store | 总线 5 条，**全部是 phase3/4 E2E 测试**（producer=phase3-e2e-test 等），最后 17:50:09 | 🟡 通但生产断流 |
| 消费者 → glue /recall | interfaces.recall | 总线仅 1 条（phase3-e2e-test 17:28:25） | 🟡 通但几乎死 |
| 主AI 插件 → glue /recall | 记忆注入 | 插件 enabled + glue healthy；无日志可观测注入频率 | 🟡 通路健康但不可观测 |
| glue → 沙漏 | 读写 | /health memory_count 3668；/recall text score 1.0 | ✅ 通 |
| glue → LMS | 激活加权 | /recall lms_activation 0.004；/health lms 2 | ✅ 通 |
| glue → 向量 | 加权召回 | /recall vector score 0.651 | ✅ 通 |
| 沙漏API → sandglass.db | memory_search | 实测返回 LIKE 命中（906 行库） | ✅ 通 |
| 沙漏API → txt 兜底 | sandglass_query | 实测返回命中（3668 行） | ✅ 通 |
| 沙漏API → 语义向量 | embedding_search | **实测返回 `{"results":[]}`**（sandglass_vault._NB 指向空目录 ~/.neurobase） | 🔴 断 |
| 沙漏API → facts.dict.md | facts_lookup | **实测报错 `No such file or directory: /vol1/@apphome/trim.openclaw/data/workspace/memory/facts.dict.md`** | 🔴 断 |
| 手机桥 → 手机网关 | memory_search | MCP 实测返回 3 条（回退路径生效） | ✅ 通 |
| 手机桥 → 手机网关 | sandglass_query | MCP 实测返回 3 条 | ✅ 通 |
| 手机桥 → 向量 | embedding_search | **MCP 实测「无结果」（query=审计）** | 🔴 断 |
| 手机桥 → facts | facts_lookup | **MCP 实测「无结果」（审计/部署 均无）** | 🔴 断 |
| LMS → 总线 | plastified/dream_complete | 502 + 740 = 1242 条（17:45→20:43），**无任何 handler 订阅** | 🟡 单向死胡同 |
| xuanjian.pipe | 占位 | 总线 1 条（17:17 测试），handler no-op 仅记录 | 🟢 死胡同 |
| 沙漏自治脉冲 | self_pulse | txt 中 self_pulse 仅 4 条，**最后一条 2026-08-02**；pulse-cron.sh 文件不存在（cron `*/10` 仍调用）；无 pacer/nexsandglass 进程 | 🔴 断（核心停搏） |
| 沙漏 MCP 8765 | 原生 MCP | 端口无监听 | 🟢 断（有替代） |
| 自愈脚本 | health-check | /tmp/pulse-status.json 自 18:20 起每 5 分钟「编辑器挂了→已重启 PID xxx」，但 **当前 edit-web 进程数=0、18888 未监听**——假阳性日志循环 | 🟡 断（谎报） |

---

## 3. 断口清单（分级）

### 🔴 致命（宣称能力整面不可用 / 故障被持续掩盖）

1. **语义检索全哑（4 个探测点全不通）**
   - 沙漏API `/api/embedding_search` → 空数组；`/api/facts_lookup` → 文件不存在
   - 手机桥 `embedding_search` / `facts_lookup` → 均无结果
   - 根因：sandglass_vault 的 `_NB` 走 `~/.neurobase`（该目录只有 4096B 空库、无 txt/idx），而真实沙漏在 `/vol1/轻如烟/轻如烟/sandglass`（同一 inode 2074605 与 /vol2 权威源相同）；`facts.dict.md` 在 NAS 侧根本不存在
   - 反差：glue 的 vector 后端本身健康（/recall 0.651）→ 是**链路配置错位**，不是能力缺失，修复成本低

2. **沙漏自治核心停搏 + 自愈机制假阳性掩盖**
   - self_pulse 最后一条 2026-08-02；pulse-cron.sh（cron 每 10 分钟）**文件不存在**；无 nexsandglass/pacer 进程 → 沙漏退化为"被动仓库"（今日 txt 仅 3 次写入：14:23/14:28 用户对话、17:50 phase4_e2e，无任何系统自产内容）
   - 同时 health-check.sh 自 18:20 起每 5 分钟记录「已重启 PID xxx」但实际 edit-web 进程 0 个、18888 未监听 → **日志与事实相反，故障被"自愈成功"掩盖**

### 🟡 显著（功能残缺）

3. **LMS 总线事件无人消费**：lms.plastified 502 条 + lms.dream_complete 740 条，7 个 handler 无一个订阅 → 单向记录死胡同（数据未丢，但"记录无人看"）
4. **生产 interfaces.store/recall 断流**：总线→胶水写入/召回只在 E2E 测试时流动（最后 17:50/17:28）；主AI→LMS/沙漏的生产写入不经过总线（glue 插件只 recall 不 store）→ 总线写链空转
5. **丰碑 audit 桥空转**：每 5 分钟 audit.py 写一条 `status=pending`，55 条无一条完成——冻结的丰碑上挂着一台永远空转的引擎（fork 冻结时未拔插头）
6. **自愈脚本三重错配**：health-check.sh / health-loop.sh / watchdog.sh 全部指向已删除路径 `/vol1/@team/qh团队/QH/AI专用/...`，且当前 `edit-web.py` 根本不监听 18888（grep=0）→ 自愈生态整体与部署脱节
7. **MCP 进程双实例**：lms-memory / lms-http / shouji 桥各 2 个（20:29 与 20:45 两批），潜在双写与资源浪费
8. **沙漏API 健康指标失真**：`sandglass_count=0`（实为 txt 3668 行 / db 906 行）→ 监控误导
9. **verify_daemon 死亡**：pid 文件残留（8/1），进程不存在，operation_log 无人校验

### 🟢 轻微（优化项）

10. xuanjian.pipe 占位无目标系统（仅 1 条测试事件）
11. 沙漏 MCP 8765 未跑（17333 + 手机桥替代成熟，建议正式废弃）
12. 丰碑自有总线 event_bus.jsonl 停在 07-16（fork 冻结佐证，可归档）
13. facts.dict.md NAS 侧缺失（手机侧也无命中）

---

## 4. 延后项重新评估（现状 → 该不该捡 → 理由）

| 延后项 | 现状（实测） | 该不该捡 | 理由 |
|--------|------------|---------|------|
| **玄鉴**（xuanjian.pipe） | 占位 handler 已注册、总线仅 1 条测试事件、无任何目标代码/目录 | **不捡（建议移除占位）** | 无真实数据源、评分无校准无消费方；"注册了却永不来事件"的占位本身是虚假接口，Phase 6 应标记 deprecated 或删除 |
| **丰碑 4 任务**（health_check/db_maintenance/freeze_check/periodic_sync） | tasks.yaml 现仅 bus_heartbeat 一个任务，4 任务不在调度中；但丰碑 audit 桥每 5min 空转写 pending | **捡一半（重定向）** | 任务框架已就绪，但指向冻结的丰碑无意义；建议把这 4 个任务重定向到 iso-sand 自身数据（operation_log/event_bus 健康检查），同时修 audit.py"永远 pending"问题或停掉空转 |
| **verify_daemon** | pid 死、依赖全在（operation_log 60+ 条、replay_dlq/compact_ids/topology 工具齐）、只缺自启 | **该捡（成本最低价值最高）** | 校验 operation_log 一致性 + 死信恢复；start_all.sh 已预留玄鉴 daemon 检查但无 verify 自启 |
| **monument_bridge**（D5） | 全库 grep 无任何代码引用，丰碑冻结 | **不捡（删除规划条目）** | 宿主系统冻结 + Windows 路径硬编码是遗留物，建议从文档删除避免误导 |
| **内置 memory_search**（database not open） | 手机桥 MCP 实测**可用**（回退到 17333 返回 3 条）；NAS BM25 的 sqlite 可能仍 not open 但被静默回退掩盖 | **轻捡（二选一）** | 修 NAS BM25 的 db 路径，或明确移除 BM25 分支只留 17333——消除"坏路径+静默回退"的隐藏故障面 |
| **沙漏 MCP 8765** | 未跑；17333 四端点实测可用 + 手机桥 sandglass_query 实测通 | **不捡（正式废弃）** | 替代已成熟；需清理 health-check.sh 中 8765 引用，避免维护幻觉 |

---

## 5. 有机性结论

### 活的循环（数据真正在流动、有闭环）
- ★ **心跳主循环**：调度器(5min) → bus → 消费者 → LMS /feed → 塑形 → 快照落盘（dream_complete 740 条、lms.feed 36 条、latest.pt 20:43 更新）——**系统的生命线，真实运转**
- ★ **LMS 做梦循环**：17:45→20:43 持续做梦，快照 424KB 随梦更新——活体特征成立
- ★ **主AI→胶水注入**：插件 enabled + glue 加权召回实测通（fail-open）
- ★ **手机桥→沙漏查询**：memory_search / sandglass_query 实测返回命中

### 死胡同（接口在/注册了，但无数据流动或无消费方）
1. lms.plastified + lms.dream_complete（1242 条）→ 无人订阅
2. interfaces.store / interfaces.recall → 仅测试流量，生产断流
3. 沙漏自治脉冲 → 停搏（self_pulse 停在 08-02）
4. 语义检索（embedding_search / facts_lookup）→ 4 探测点全哑
5. 丰碑 audit pending 记录 → 永无完成方
6. xuanjian.pipe → 占位无目标
7. 自愈脚本 → 每 5 分钟假阳性循环

### 有机性评分：**6 / 10**
> 骨架健康：总线/消费者/胶水/LMS/网关之间数据真正在流动、有闭环、有快照演化——"会心跳、会做梦、会反哺的活体"这句评价**成立**。
> 但**感知层全盲**（语义/事实检索 4 点全哑）、**自治层停搏**（沙漏核心死 + 自愈谎报）、**外围空转**（丰碑审计桥、玄鉴占位、lms.* 死胡同）——三分之一的注册接口是孤岛或假活。

---

## 6. 给 dandan 的一页结论

### TOP5 必须补的断口
1. **语义检索双哑（🔴）**：给 17333 API 进程设 `NEXSANDBASE_HOME`（或修 sandglass_paths._NB 指向真实沙漏目录）+ 重建 facts.dict.md 路径 → 一次修好 4 个哑点，向量/事实感知面复活（glue 的 vector 后端本来就健康，纯链路错位）
2. **沙漏自治核心复活（🔴）**：补 pulse-cron.sh（或拉起 nexsandglass.py 常驻进程）→ self_pulse 恢复，沙漏从"仓库"变回"活体"
3. **自愈脚本重写（🔴）**：health-check.sh/health-loop.sh/watchdog.sh 指向当前路径 `/vol1/轻如烟/轻如烟/scripts/edit-web.py`、修正端口、删除假阳性"已重启"日志——每 5 分钟一次的谎言必须停
4. **死胡同清理（🟡）**：lms.* 事件接入观察/归档；丰碑 audit.py 补"完成"侧或停掉空转（每 5min 写一条 pending 永无下文）
5. **MCP 双实例清理 + 生产写链确认（🟡）**：lms-memory/lms-http/shouji 各留单实例；确认主AI→LMS/沙漏的生产写入是否真实发生（总线看不到，需在插件/胶水加审计日志）

### 建议现在捡起的延后项
- **verify_daemon**（依赖全在、只缺自启，成本最低价值最高）
- **丰碑 4 任务重定向**（改为校验 iso-sand 自身 operation_log/event_bus）
- **内置 memory_search 二选一**（修 NAS BM25 路径或删 BM25 分支）

### Phase 6 应优先解决的部署断口
- `NEXSANDBASE_HOME` 环境变量统一（glue 行为正常而沙漏API 异常的根因）
- 8765 正式废弃 + health-check 引用清理
- MCP 进程单实例化（防双写）
- 沙漏API `/api/health sandglass_count` 失真修复（消除误导指标）
- **自启清单核对**：start_all.sh 目前只启调度器+消费者，glue/LMS/沙漏API 均手工拉起且无统一管理；verify/玄鉴自启位是死路径——Phase 6 应产出一份"进程→自启方式→健康探针"全量清单

---

*本报告全部结论附实测证据（命令输出摘要/时间戳/计数），未做任何修改性操作。*
