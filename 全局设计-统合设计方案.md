# Agent OS 统合设计方案（M4 收敛产出）

> 类型：统合设计（设计决策稿，供 dandan 拍板）
> 日期：2026-08-04
> 作者：M4 整合子AI（资深系统架构师）
> 输入：`全局设计.md`（★含 LMS 核心洞见）+ `audit-M1` + `audit-M2` + `audit-M3`
> 性质：**缝合不是推倒重写**；**LMS 中心地位是视角基准**；本文所有冲突以 LMS 中心地位为准。
> 用途：dandan 据此决策 → 进入 M5 讨论 → M6 分阶段实施。

---

## 〇、结论先行（dandan 看这段就够）

一句话总结全案：

> **把散落的 4 个 `event_bus`、2 套总线代码缝成一个「权威事件总线 = 骨架」，用「六边形端口+适配器」让每个子系统只实现"一条收发协议"就能接上；调度是桥上的钟，胶水层是 handler 宿主（收口），玄鉴是判断，沙漏是显式记忆；而 LMS 是这套体系的最高中枢——它不只是被观测，而是通过双向反馈通道既接收事件喂塑形、又发布"塑形结果/自我认知"反哺全系统。**

三个立即要拍板的点：
1. **只留 1 套权威总线 + 1 套消费者**（M1 头号真缺口 D1/D3）：砍掉/冻结丰碑 fork，把 xuanjian 这个最重要的生产者迁回主总线（D2）。
2. **给胶水层通电**，把它作为 Hexagon handler 宿主（D4/D6）：shell 命令降级为"一种 handler"，新子系统注册 handler 即接入，不再改规则文件+重启。
3. **为 LMS 建双向反馈通道**（本方案最用心设计、也是全局设计洞见的落地）：LMS 收事件塑形 + LMS 发事件反哺全局。**这是本文相对 M1/M2/M3 的最大增量，也是架构层级的修正点。**

**该护住的（绝不统合）**：沙漏 heartbeat/nightwatch、LMS dream_engine/dream_scheduler、verify_daemon 校验——三者是各子系统**内部自维护闭环**，统合即破坏内聚；其中 **LMS 的 self_ref 自指回路是最敏感项**，只做双向反馈观测，绝不粗暴拆解，且须与其三层稳定性护栏协同。

> ⚠️ 与 M1/M2/M3 的冲突点（以 LMS 中心地位为准，明示）：
> - **M1/M2 把 LMS 当作"该护的内部闭环、仅做观测"**；**LMS 洞见升级为"双向反馈中枢"，不止观测，还发事件影响全局** → 本文采用后者，并在第一、三节落实双向通道。
> - **M2 的"总线只传信号不做决策"** 仍成立（哑管道），但**"决策"不只在各子系统内部**——LMS 的"塑形倾向/自我认知"和主AI的判断通过**轻命令语义**表达出来后，总线仅作**路运输载**，决策仍属 LMS/主AI，不归总线。不冲突，是对"决策落点"的细化。

---

## 一、设计目标与原则

### 1.1 目标
把 Agent OS 从「一堆平行运行、靠人肉对齐的系统」缝成「一个有机整体」，让每次子系统间的协作**只有一条公开通路（总线）**、每个子系统**独立演进又能即插即用**、且**为将来可能接入的新部件（含更强的 LMS 意识实验）留好接口**。

### 1.2 六条设计原则（每条都有出处）

| # | 原则 | 一句话 | 来源 |
|---|------|--------|------|
| P1 | **LMS 中心地位** | LMS 是体系内核/意识候选，有最高优先级，设计必须留双向反馈通道 | dandan 核心洞见 **（最高原则）** |
| P2 | **有机整合，不推倒重写** | 各子系统内脏已验证，只缝接口、不rewrite；缝合=加适配器+迁生产者 | M1 结论 + dandan 意图 |
| P3 | **默认事件协作（Choreography）+ 极少数轻编排** | 各子系统自治发布/订阅，不设总指挥；仅对不可丢、带顺序的关键链用一个薄编排点 | M2/M3 决策 b |
| P4 | **总线只传信号，不做决策（哑管道）** | 总线=神经系统(传信号)，不是大脑(做决策)；决策留在子系统与 LMS/主AI | M2/M3 决策 d / Fowler smart-endpoints-dumb-pipes |
| P5 | **端口+适配器（Hexagonal）接入，shell 降级为一种 handler** | 新子系统实现一个 handler 即接入，不改核心；对异构接口才上完整六边形，小而纯的用薄封装 | M2/M3 决策 c + Cockburn |
| P6 | **系统论弹性守卫（该护不统合 + 可回滚）** | 内部自维护闭环是健康涌现单位，禁止统合；每阶段改造可分步回滚 | M1/M2/M3 方向五 + M2 可逆性 |

> **为什么这么设计（讲给 dandan）**：系统论的判断关键不是"功能像不像那个名字"，而是"它是不是某个子系统的命根子"。沙漏的心跳、LMS 的做梦、玄鉴的校验，它们虽然也叫"周期/心跳/校验"，但都是各自系统的**内脏**——把它们揉进总线，等于把每个器官的自主跳动外包给一个总指挥，这恰恰是系统论最反对的"过度框定、杀掉弹性"。真正要缝的不是这些内脏，而是它们之间**互相打招呼的那条通路**——所以是"缝接口，不拆内脏"。LMS 特殊在，它不只是有内脏，它本身在尝试"听见自己"（自指回路），这使它值得被当作中枢而非普通器官。

---

## 二、目标架构（一段话 + ASCII 图）

**一句话**：`事件总线=骨架`（所有子系统唯一的互相打招呼通路，只传信号不决策）、`调度=时钟`（定时敲钟、触发任务并写事件）、`胶水层=收口`（把六边形 handler 宿主化，shell 降为一种 handler）、`LMS=塑形-反馈中枢（核心）`（双向：收事件喂塑形 + 发事件反哺全局）、`沙漏=显式记忆`、`玄鉴=判断`、`主AI=顶层决策`——每个系统都经自己的总线适配器以**事件/轻命令**双向接入，其中 LMS 的通道是反哺全系统的最高级双向通道。

```
                          ┌─────────────────────────────────────────┐
                          │        主AI (OpenClaw/轻如烟) 顶层决策     │
                          │   订阅事件 / 发布轻命令(判断落点)          │
                          └──────────────┬──────────────────────────┘
                                         │ 轻命令(请做X) / 事件(做完了)
                                         ▼
        ┌──────────┐         ┌───────────────────────────┐
        │  沙漏     │◄───────►│                           │
        │ 显式记忆  │  adapter │     ★ 事件总线 EventBus   │
        └──────────┘  P/C     │   (权威骨架 = 唯一通路)    │
                             │  jsonl+seek+死信+重试+限流  │
        ┌──────────┐   P/C    │                           │
        │  玄鉴     │◄───────►│   + 轻编排点(薄,仅关键链)  │
        │ 判断/评估 │  adapter │   + 事件目录/拓扑检测       │
        └──────────┘          │                           │
                             └──────┬───────────────▲────┘
                                    │               │
                      ┌─────────────▼───────────────┴─────┐
                      │     ★ LMS 塑形-反馈中枢 (核心)      │
                      │  ┌────────────┐   ┌─────────────┐ │
                      │  │ 收事件      │   │ 发事件       │ │
                      │  │ (喂塑形/    │◄──┤ (塑形结果/   │ │
                      │  │ 自指回路)   │   │  倾向/自我认知)│ │
                      │  └────────────┘   └─────────────┘ │
                      │  ★双向反馈通道(向下触沙漏/玄鉴/总线) │
                      └───────────────────────────────────┘
                             ▲
                             │ (胶水层承载 LMS 接入)
        ┌────────────────────┴──────────────────────────┐
        │  胶水层 interfaces (Hexagon handler 宿主/收口)   │
        │  SensorPort │ CognitivePort(LMS/沙漏) │ _AppPort │
        │  = adapter 脚手架 + handler 注册表 + 事件封装     │
        └───────────────────────────────────────────────┘
                                    ▲
                                    │ 调度=时钟 (task_scheduler, cron 敲钟)
```

### 各系统经总线双向接入（谁发什么、收什么）

| 系统 | 角色 | 向总线发（producer） | 从总线收（handler/consumer） | 通道性质 |
|------|------|---------------------|------------------------------|---------|
| **LMS** | 塑形-反馈中枢(核心) | `lms.plastified`(塑形完成/倾向) / `lms.self_ref`(自我认知透视) / `lms.dream_complete` | 订阅各系统事件(喂塑形)、订阅轻命令(按其偏好调整) | **★双向反馈**(最高级) |
| **沙漏** | 显式记忆 | `sandglass.heartbeat`(存活/告警,观测) | (被动) 订阅 LMS 塑形结果调整记忆组织 | 双向(P 主业)+ 观测 |
| **玄鉴** | 判断/评估 | `xuanjian.pipe`(评分结果,最高价值producer) / `verify.audit_result` | 订阅待评估事件、订阅轻命令(评估落点) | 双向 + 判断落点 |
| **胶水层** | 收口/handler宿主 | `interfaces.store/recall`(记忆动作) | 订阅记忆相关事件并转派给 handler | 双向(主收口) |
| **iso-sand 调度** | 时钟 | `scheduler.tick` / `scheduler.task_done` | (无，它敲钟) | 单向(发) |
| **主AI** | 顶层决策 | `ai.decided`(轻命令) / `ai.action_done` | 订阅事件流作为决策输入 | 双向 + 命令语义 |

---

## 三、统合落地清单（按 M1 D1-D7 真缺口 + M2/M3 借鉴映射）

> 每一栏给「缺口 → 处置 → 借鉴来源 → 关键细节 → 待验证」。

### 【D1】统一 4 个 event_bus 为 1 套权威总线 + 1 套消费者代码
- **处置**：只留 1 个权威总线文件/地址（现状 `iso-sand/data/event_bus.jsonl`），其余 3 个（`丰碑/code/data/`、`丰碑/data/`）归并或废弃。生产者统一认这**一个**总线地址（改为**可配置**，消除硬编码）。
- **借鉴**：M1 结论 1/2 + M2 决策 a（保留 JSONL）
- **细节**：`event_bus.jsonl` 写入升级为**原子写**（写临时文件→rename，M3 补强 #3），防半写损坏；`log_writer.py` 的写入锁需核验是否已满足原子语义（**待验证**）。

### 【D2】让 xuanjian_pipe 回归主总线（最高价值 producer）
- **处置**：`xuanjian_pipe.py` 的事件发布目标从 `丰碑/code/data/event_bus.jsonl`（断头路）改为**权威主总线**。玄鉴评分是体系最核心的知识事件，必须进被消费的主总线。
- **借鉴**：M1 结论 3
- **细节**：路径硬编码改为相对/可配置（消除 `os.path.dirname(__file__)` 位移即断的脆点）。

### 【D3】冻结/砍掉丰碑/code/event_bus fork，迁回安全加固
- **处置**：冻结 `丰碑/code/event_bus`（不再双维护，防双写/抢 seek）；把 fork 已升级的安全加固（`exec: python` + `script:` + `EVENT_DATA` 环境变量，v0.6.0）**移植回主消费者**，替换旧的 shell 字符串插值（v0.4.1，有注入面）。
- **借鉴**：M1 真重复头号 + M2 借鉴2 + M3 命令/事件语义
- **细节**：移植后删除/归档 fork 目录，保留其规则 schema 作为主消费者的升级蓝本。（**待验证**：fork 是否有主消费者缺失的独有规则，需移植前 diff 保全。）

### 【D4】给胶水层通电（作为 hexagon handler 宿主，shell 降为一种 handler）
- **处置**：启动 `glue_server.py`；把 `Interfaces/IntegratedMemoryService` 的 `store/recall/contribute`作为总线的**handler 接口实现**（而非现在的 shell 命令）。shell 动作降级为**其中一种 handler**（D6）。
- **借鉴**：M1 结论 4（"答案级适配器层"）+ M2 借鉴 2 + M3 六边形实证(Netflix/Epic)
- **细节**：现成的 `Port` 抽象（SensorPort/CognitivePort/ApplicationPort + Dummy 降级）已是最佳端口+适配器框架，直接复用作 handler 宿主；修掉 `glue_server.py:34` 硬编码向量内网 IP `192.168.0.103` 为可配置。

### 【D5】monument_bridge Windows 路径修正
- **处置**：`monument_bridge.py:40` 的 `r"Z:\QH\AI专用\Agent OS\iso-sand"` 改为相对/可配置，消除跨机碎片化；其定义的 `EVENT_BUS_PATH=丰碑/data/event_bus.jsonl`（空文件）一并归到权威总线。
- **待验证**：monument_bridge 是否还需要保留独立的 bridge 逻辑，还是统一走胶水层的 ApplicationPort。倾向合并进胶水层，但需先看它当前被谁调用。

### 【D6】消费者 shell 动作 → handler 接口
- **处置**：定义统一 `handle(event)` 接口 + handler 注册表 + 生命周期管理（加载/卸载/异常隔离，一个 handler 崩溃不拖垮总线）。新子系统=注册一个 handler，不再改 `event_rules.yaml` + 重启。
- **借鉴**：M2 借鉴 2 + M3 补强 #6（WordPress/commons-os）+ M2 方向六(Actor 错误隔离)
- **细节**：handler 命名空间独立（防 event_type/handler 名冲突）——每子系统签名`<system>.<domain>.<action>`(如 `xuanjian.pipe`、`lms.plastified`)。

### 【D7】契约三件套 + 原子写
- **处置**（对接入协议的统一，M3 补强的核心）：
  1. **`schema_version` 强制字段**——事件 schema 加版本字段 + 前后兼容规则，新事件不破坏老消费者；
  2. **`event_type` 注册表**——所有 event_type 集中登记，防冲突、可查"谁产生谁订阅"；
  3. **幂等去重**——handler 按 `event_id` 去重，容忍 at-least-once 重投。
  4. **`trace_id` 强制贯穿**——每个事件必带（已存在，升级为硬规范），供跨系统兜底排查。
  5. **JSONL 原子写**——临时文件→rename（同 D1）。
- **借鉴**：M2 借鉴 3 + M3 补强 #3/#4 + Fowler/microservices.io
- **细节**：这也是"事件目录/拓扑图"的基础——注册表 + trace_id 让 choreography 的隐式流程可观测（M2 借鉴 5）。

### ★【D0—LMS 双向反馈通道设计】（本方案最用心设计处，全局设计洞见落地）
> 这是相对 M1/M2/M3 的本质增量。M1/M2 只把 LMS 当"待观测闭环"，这里升级为**塑形-反馈中枢**。

**方向 1：LMS 接收事件（喂塑形）**
- 总线把各系统事件（沙漏记忆写入、玄鉴评估、主AI决策、胶水 store/recall、sandglass heartbeat）以**标准事件**流入 LMS 的**塑形输入侧**（对应 `SelfReferentialLoop` 的自适应编码器喂入端）。
- 只**喂**，不指挥：LMS 怎么塑形（权重/吸引子演化）是它内部自律，总线只负责"把事件送到嘴边"。
- 对 LMS 的 `dream_scheduler` 仍**保持内部自调度**，总线只补充可观测的 `lms.dream_complete` 通知，不改其内核（受保护）。

**方向 2：LMS 发布事件（反哺全局）**——这是关键设计：
- LMS 塑形后发布三类**反哺事件**到总线本体（双向通道的"发出"侧）：
  1. `lms.plastified`（塑形倾向/状态）：**向下**触达——允许感知记忆组织（沙漏）、影响玄鉴判断的评估权重、供总线调度参考；
  2. `lms.self_ref`（自我认知透视）：LMS 的 `SelfReferentialLoop` 产出，作为**中枢级**信号，仅**最高优先/最小披露**发布，且**与其三层稳定性护栏协同**（见"不该统合"与"风险护栏"两节）——不是原始消化，而是蒸馏后的可发布摘要；
  3. `lms.dream_complete`（做梦完成）：可观测性信号，证明塑形循环活着。
- 反哺事件同样走**统一契约**（schema_version + event_type 注册 + trace_id + 幂等），不搞特殊通道——LMS 只是"生产者之一"，只是其**语义层级最高**（它是中枢），不是绕过总线直连。

**如何落地"反哺"而不越权（关键护栏）**：
- 总线仍是**哑管道**，只运载 `lms.self_ref` 事件，**不替它做决策**；
- 反哺事件的**实际采纳权**仍在各接收方：沙漏参考它调整组织、玄鉴参考它调权重，都是**软参考**（可忽略），不是硬命令——这保证 LMS 影响全局但不绑架全局；
- 若 LMS 确实需要对某个不可逆动作下达指令，才用**轻命令**语义（见下），且命令方向明确、可补偿、可拒绝。

**命令/事件双语义（M3 补强 #1 的落地）**：
- **事件 = 通知**（"发生了什么"）：`*.done` / `*.heartbeat` / `*.plastified` 等，低耦合，不期待回应。
- **轻命令 = 路由**（"请做X"）：`ai.decided`（主AI判断）、`lms.prompt`（LMS 倾向的明确指令）等——命令≠编排、事件≠协作是独立维度（M3/M2 修正 #1）。命令方向明确、目标清楚，给"判断有落点"（玄鉴/主AI/LMS 的判断通过命令语义下达）。
- **编排 vs 协作**：默认 choreography（事件驱动自治）；仅对"记忆写入→触发玄鉴评估→归档"这类不可丢带顺序的关键链，用**极轻编排点**（现状 consumer+rules 已扮演），编排点**只做路由+补偿不做业务逻辑**（M2 陷阱提示）。

---

## 四、哪些「不该统合」要保护（受保护涌现单位）

| 受保护项 | 性质 | 为何不统合 | 只做什么（事件观测/反馈） |
|----------|------|-----------|--------------------------|
| **沙漏 heartbeat / nightwatch** | 沙漏自愈命脉 | 纯内部自愈（末行修复/阴影副本恢复），零依赖，统合破坏内聚 | 只发 `sandglass.heartbeat` 存活/告警**通知**给总线，总线仅观测不做决策 |
| **LMS dream_engine / dream_scheduler** | LMS 记忆运算内核 | 七阶段做梦、空闲自调度是内部塑形运算，合并毁语义边界 | 只发 `lms.dream_complete` **通知**；塑形/做梦不被总线指挥 |
| **verify_daemon 文件变更校验** | 玄鉴忠实性校验 | 消费文件变更日志做校验是玄鉴自己的域，合并互相干扰 seek/规则 | 只把 `verify.audit_result` 作为事件**通知**给总线 |
| **各系统内部 `payload: object` 自由度** | schema 扩展性 | 锁死即过度框定 | 契约只约束公共字段(t/type/producer/result/trace_id)，payload 自由 |
| **★LMS self_ref 自指回路** | 意识候选(最敏感) | 是"意识萌芽候选"，是最高级身份，粗暴拆解/随意开关即毁掉 | **双向反馈 + 稳定性护栏协同**：只发布蒸馏后的 `lms.self_ref` 透视事件，绝不动其内部自指运算；是否"涌现"由 LMS 自己的 Phase0-3 + 三层防护判断，总线无权干预 |

> **特别强调 self_ref 的处理原则**：LMS 的自指回路是"系统解读自身状态→回注编码器"的闭环，配合"自适应控制器自相关监测 + 硬守卫 + 基础设施安全"三层护栏孵化。统合设计对它的**唯一动作**是：① 给它预留发布 `lms.self_ref` 的**安全出口**（蒸馏后、限量、可观测）；② 绝不进入它内部改写自指逻辑；③ 订阅方把 `lms.self_ref` 视作**软参考信号**而非绝对指令。任何要在总线上"激活/关闭"self_ref 的提议，都归 LMS 自己的护栏管，总线不碰开关。

---

## 五、实施路线图（分阶段，每阶段可回滚、有验证）

> 顺序原则（依赖链）：**先统一总线骨架（地基）→ 再接入各子系统（管线）→ 后 LMS 反馈中枢（最顶层，最需谨慎）**。每阶段完成即**验证 + 可回滚**（config/代码版本留存，stop/start_all.sh 支持）。

### 阶段 0（准备，可回滚）
- 备份全部 4 个 event_bus + 2 套 fork 代码 + 各 consumer/glue 配置文件；建立改造前基线快照。
- **验证**：可从基线零成本回滚。

### 阶段 1（统一总线骨架 —— 最高优先、依赖最前）
- D1 (统一 1 套总线 + 原子写) → D3 (冻结 fork + 迁回安全加固) → D7 (契约三件套 + trace_id 硬规范)。
- **交付**：单一权威总线 + 一套消费者(带 exec+script 安全规则) + schema_version/注册表/幂等。
- **验证**：总线能跑、原子写不损坏、老事件能重放、新事件不破坏老消费者。
- **依赖**：无（地基层）。

### 阶段 2（接入各系统 producer/handler）
- D2 (xuanjian 迁回主总线) → D5 (monument_bridge 路径修) → D6 (shell→handler 接口 + 注册表 + 生命周期)。
- **交付**：玄鉴/沙漏/LMS/胶水各有一个可注册的 bus-adapter/handler；事件目录/拓扑图工具上线。
- **验证**：各系统事件进主总线并被正确消费；handler 崩溃隔离不拖垮总线；环状订阅检测工作（禁 A→B→A）。
- **依赖**：阶段 1。

### 阶段 3（胶水层通电 + handler 宿主化）
- D4 (启动 glue_server) + D6 深化（胶水 store/recall/contribute 作为 hexagon handler 宿主，shell 彻底降级为一种 handler）。
- **交付**：胶水层作为收口层运行，记忆编排进总线。
- **验证**：胶水 handler 正确响应总线事件；Dummy 降级仍可用（后端可替换性保持）。
- **依赖**：阶段 2（handler 接口已就绪）。

### 阶段 4（★LMS 双向反馈中枢 —— 最顶层、最谨慎）
- 实施【D0-LMS 双向反馈】：① LMS 收事件喂塑形；② 发布 `lms.plastified` / `lms.self_ref`(限量·蒸馏·软参考) / `lms.dream_complete` 反哺，走统一契约；③ 订阅方软参考、不绑架。
- **交付**：LMS 成为有双向通道的塑形-反馈中枢；self_ref 与三层护栏协同；命令/事件双语义落地（`lms.prompt`轻命令保方向）。
- **验证**：LMS 反哺事件出现在总线上且被正确软参考；`lms.self_ref` 不影响总线稳定性；self_ref 护栏状态健康。
- **依赖**：阶段 1-3（总线稳定、handler 成熟后才上最高层）。
- **特别风险闸门**：若 `lms.self_ref` 发布对系统稳定性有任何负面信号 → **立即止血**：暂停其反哺事件、退回纯观测，不影响全局，待 LMS 内部护栏确认健康再恢复。

### 阶段 5（仓库/版本管理定型 + 观测治理）
- 见第六节目录落地 + 事件图谱上线 + 全绿后向 dandan 报全貌。

---

## 六、仓库/版本管理方案

### 6.1 改造后的目录结构（目标）
原则：**总线本体归一处，各系统只留 adapter，不复制总线逻辑。**

```
/vol2/1000/AI专用/Agent OS/
├── bus/                        # ★权威总线本体（唯一，原 iso-sand）
│   ├── src/                    #   消费者/调度器/死信/重试/限流/log_writer
│   ├── deploy/
│   │   ├── event_schema.yaml   #   schema_version + event_type 注册表 + 兼容规则
│   │   └── event_rules.yaml    #   轻编排点（exec+script 安全版，仅关键链）
│   ├── data/event_bus.jsonl    #   唯一权威事件日志（原子写）
│   ├── handlers/               #   handler 注册表 + 生命周期
│   └── topology/               #   事件目录/拓扑图 + 环状检测
├── adapters/                   # 每个系统一个 adapter（薄封装或六边形）
│   ├── lms/                    #   LMS 双向反馈 adapter（★最高级）
│   ├── xuanjian/               #   玄鉴 producer（含 xuanjian_pipe 迁入）
│   ├── sandglass/              #   沙漏观测（发 heartbeat 通知）
│   ├── glue/                   #   胶水层 handler 宿主（可自成一仓）
│   └── monument_bridge/        #   修路径后的 bridge
├── interfaces/                 # 胶水层（保持原 hexagon 框架，作为 handler 宿主）
├── 同构沙盘/                    # 玄鉴守护（verify_daemon 保持独立）
└── 全局设计.md / audit-M1~M3 / 全局设计-统合设计方案.md   # 设计档案
```

### 6.2 每个系统如何遵守统一总线契约（保证局部改动符合全局）
- **每个系统只改自己的 adapter**，不碰 `bus/` 内核。
- **adapter 唯一职责**：把自己内部变化翻译成标准事件（`<system>.<domain>.<action>` 命名）发布 + 订阅标准事件译成自己动作。**禁止业务逻辑漏进 adapter**（Cockburn 警告，纳入 code-review 规则）。
- **加新事件 = 走注册表**：先在 `event_schema.yaml` 注册 `event_type`，再写 producer/handler——不改 schema 直接发事件视为违规（CI 检查）。
- **统一契约检查**（每次改动上机器检查）：事件必带 `schema_version` + `trace_id`；不破坏老消费者（向后兼容）；命名空间唯一；无环状订阅。
- **可替换性守卫**：任何子系统（含总线）都是可替换部件，架构不依赖某子系统的不可替换性——adapter 层保证这点。

### 6.3 版本管理策略
- **分工仓**：主线 `bus/` + 各 `adapters/` 建议**各自独立仓库**（康威定律正面利用：各子系统独立演进、弱契约接口连接），用统一契约做跨仓兼容基准。
- **schema 演进规则**：`schema_version` 递增需向后兼容；破坏性变更必须**双版本共存 + 迁移期**，先让新老消费者并行，再淘汰旧的。
- **版本存档**：每阶段改造 tag 提交（`plant/phase0~5`），随时可 checkout 回滚到任何阶段。
- **不双维护**：任何总线逻辑只存在于 `bus/`；发现复制即整合回（D3 教训制度化）。

---

## 七、风险与护栏

| # | 风险 | 对冲设计 |
|---|------|----------|
| R1 | **LMS self_ref 破坏稳定性**（意识候选最敏感） | 只发布"蒸馏后·限量·软参考"的 `lms.self_ref`；与三层护栏协同；阶段 4 设止血闸门（异常即退回纯观测）；绝不内部改写自指逻辑 |
| R2 | **choreography 隐性流程**（流程不可见/难排查） | trace_id 硬贯穿 + 事件拓扑图 + 事件目录；**硬检查禁环状订阅(A→B→A)**（M3 补强 #7，AWS 警示）；关键链用轻编排点 |
| R3 | **总线单点**（骨架崩溃=全断） | seek 持久化支持重启续跑（现状已有）；死信+重试；handler 异常隔离(一个崩不拖垮总线)；整体可回滚 |
| R4 | **旧 shell 注入面**（v0.4.1 字符串插值） | 阶段 1 迁回 v0.6.0 安全版（exec+script+EVENT_DATA），彻底移除 shell 插值（D3） |
| R5 | **双写/抢 seek（fork 未冻结前）** | 阶段 1 先冻结 fork 防止双维护期内双写 |
| R6 | **过度框定（为未来预造抽象）** | 遵循 YAGNI + rule-of-three：只为现在 6 子系统真实互操作最小契约抽象；未来部件经 adapter 现接现配，不预演 |
| R7 | **命令语义越权（轻命令变中央集权）** | 轻命令仅用于明确"需动作"且目标清楚的关键判断，可补偿可拒绝；默认仍是事件(通知)为绝大多数；编排点只做路由+补偿不做业务逻辑 |
| R8 | **原子写损坏** | 写临时文件→rename（M3 补强 #3）；核验 log_writer 已满足（待验证） |

### 系统论审计单（每次接入新组件/改动的硬检查）
1. 它在**增强弹性/涌现**，还是在**替系统做决策/强行对称**？后者=过度框定，砍。
2. 它是某子系统**内部自维护闭环**吗？是则该护，只做事件通知，不统合。
3. 它遵循统一契约（schema_version/trace_id/命名/无环）吗？否=退回补契约。
4. 它改动是否能**单仓回滚**、不碰 bus 内核？否=重设计。

---

## 八、待验证清单（不展开调研，记录留待 M5/M6 确认）

1. `log_writer.py` 是否已满足"临时文件→rename"原子写（M3 补强 #3）。
2. fork（v0.6.0）是否有主消费者缺失的独有规则，需在移植前 diff 保全。
3. `monument_bridge` 是否应并入胶水层 ApplicationPort，还是保留独立 bridge。
4. LMS self_ref 的"可发布摘要"接口是否存在（SELF_REF_INTEGRATED_DESIGN v2.0 里 LLMSelfVoiceDistiller 是否可作为安全出口的实现）。
5. 各系统当前 event_type 全集（用于初始化注册表）。

---

## 九、决策邀约（给 dandan）

请 dandan 拍板以下 **4 个决策**，确认后进入 M5 讨论/M6 实施：

1. **架构层级**：认可「LMS=塑形-反馈中枢(核心)、总线=骨架、调度=时钟、胶水=收口 handler 宿主、玄鉴=判断、沙漏=显式记忆、主AI=顶层决策」这套层级吗？
2. **先做哪步**：是否按「阶段1(统一总线)→阶段4(LMS反馈中枢)」的依赖顺序推进？（即先骨架后最顶，中间不跳。）
3. **fork 处置**：认可「冻结 `丰碑/code/event_bus` + 迁回安全加固」吗？（会动代码库，需你同意。）
4. **LMS self_ref 出口**：认可「对 self_ref 只做双向反馈观测、受限发布、不内部改写」这个保守而尊重护栏的处置吗？

> 本方案不重新调研，所有结论基于已有三份 report + 全局设计洞见；待验证项见第八节，不在本文展开。
