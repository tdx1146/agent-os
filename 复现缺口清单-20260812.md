# 复现缺口清单-20260812.md — 「只靠 GitHub 完整复现」审计

> 背景：dandan 要求「姐姐/其他人只通过访问 GitHub，就能完整在本地复现我们所有的状态」。
> 方法：站在姐姐/新人角度，只 clone 各 GitHub 仓库，逐项验证能否复现生产行为。
> 审计日期：2026-08-12。本清单随 agent-os 仓维护；处置状态以本文件为准。

---

## 总览（先看这个）

| # | 缺口 | 严重度 | 状态（2026-08-12） |
|---|------|--------|-------------------|
| 1 | **沙漏 nyx 漏推失忆根因三件套**（GitHub 只有 4 个 P0 补丁，本地 8 commit + 未提交生产态） | 🔴 阻断 | ✅ 已修：已 commit + 推 tdx1146/nyx main（现 9 commit 全量） |
| 2 | **玄鉴 verify_daemon 无任何远端仓库**（本机无 .git，GitHub 无对应仓） | 🔴 阻断 | ✅ 已修：已并入 agent-os/xuanjian（2026-08-12） |
| 3 | **edit-web.py 仓 main 分叉 20 commit**（梦醒/dashboard 等生产行为不在 GitHub main） | 🟠 高 | ⏳ 本地已 commit wrapper 修复；推送需合并决策 |
| 4 | **glue-memory-injector 仓与生产分叉**（storeTurn 开关定案未推，远端另有平行历史） | 🟠 中 | ✅ 已修：merge + 推送生产态（81e5257） |
| 5 | **编辑器落沙 wrapper 硬编码本机绝对路径**（姐姐机器上落沙必挂=明线断） | 🔴 阻断 | ✅ 已修：改 LIGHT_HOME env + 相对推导（edit-web 仓 commit 17799d0，未推） |
| 6 | **env.template 缺 WAKE_CHANNEL/SG_*/LMS_FEED_***（姐姐部署后唤醒/塑形参数缺省） | 🟡 低 | ✅ 已修：env.template D 节补齐（agent-os） |
| 7 | **LMS .env.example 缺 FALLBACK_URL/CONTROL_TOKEN/DREAM_*** | 🟡 低 | ⏳ 待补（LMS 仓，小改动） |
| 8 | **文档引用本机绝对路径/旧入口**（SYSTEM.md/DEPLOY-GLOBAL 部分段落新人难懂） | 🟡 低 | ✅ 已修：SYSTEM.md §3/§3.4/坑6/组件表 + DEPLOY-GLOBAL §2 + TOPOLOGY §6 |
| 9 | **qingruyan-scripts 与 edit-web.py 仓内容重叠**（self_pulse 脚本两份，易混淆） | 🟡 低 | ⏳ 建议废弃标注或合并（待 dandan） |
| 10 | **dashboard.html 是生成物却入库**（gen_dashboard 每 5min 重写→脏树） | 🟢 提示 | ⏳ 建议 .gitignore（改现有需申报） |
| 11 | **start_all.sh / stack_ctl.sh / deploy.sh 三入口并存**（新人认知负担） | 🟢 提示 | ✅ 已修：文档明示分层关系 |

---

## 逐项详情

### #1 沙漏 nyx 漏推失忆根因三件套 🔴 → ✅ 已修

**现状（审计时）**：`tdx1146/nyx` main = `50aec14`（仅 P0-1/2/3/P0-2b + 补丁说明 4 个 commit）；本地 `轻如烟/sandglass_source` = `ba568e6`（8 commit）+ 未提交生产态（sandglass_vault.py +18、sandglass_mcp.py +175）。

**缺口**：姐姐 clone tdx1146/nyx 拿到的是**没有失忆根因三件套**的版本：
- `84e2c1f` FTS 冻结修复（schema 规范化 + txt 权威去重回填 + 写锁立即提交）
- `3d7ea05` 搜索工具修复（查询前增量同步/多关键词 OR/FTS5 优先/shouji 本机直连）
- `ba568e6` _parse_entries 时间戳校验（脏行修复）

**影响**：部署出来的沙漏正是我们 8/11 刚修掉的"关掉窗口就失忆"状态——FTS 冻结、搜索坏、脏行误判。系统"活着但失忆"。

**处置（已做）**：
1. 未提交生产态入库：`sandglass_vault.py`（FTS 增量索引推进指针+缓存短路）、`sandglass_mcp.py`（单实例锁 + read_backlog/web_search/openalex_search/self_pulse 工具；workspace 路径改 `WORKSPACE_HOME` 环境变量可配，默认本机路径向后兼容）
2. PATCH-README 补丁清单 4→8 commit + 新增工具说明 + 数据不随仓分发声明
3. **已推** `tdx1146/nyx` main = `2fd6b28`（9 commit 全量，2026-08-12 16:40 验证）

### #2 玄鉴 verify_daemon 无远端 🔴 → ✅ 已修（2026-08-12 并入 agent-os）

**现状（审计时误报）**：`/vol2/1000/AI专用/AgentOS-IsoSand/同构沙盘` 无 .git（误报原文）；`github.com/tdx1146/AgentOS-IsoSand` 不存在（ls-remote 需认证=不存在/私有）。**更正**：同构沙盘是本地 git 仓（master，历史 f61c39e/f12c79e/78452ea 等），只是**无 remote**；此前"玄鉴无远端需新建仓库"的判断错误——正确做法是并入 agent-os 统一维护（dandan：代码不能散沙）。

**处置（已做，2026-08-12）**：
1. 玄鉴核心（`src/` 6 件：verify_daemon/ci_verify/essence_distiller/facts_manager/verify_claim/iso_logger + tests/ + deploy/ + config/）复制到 **`agent-os/xuanjian/`**，随 `tdx1146/agent-os` 仓分发；新目录从零跟踪（不合并历史，源仓保留为运行实例与历史参考，来源与关键 commit 注明在 `xuanjian/README.md`）
2. `verify_daemon.py` 路径参数化（`XJ_KERNEL_SPEC_DIR`/`XJ_DOUBT_HOOK`/`XJ_REPO_LMS`/`XJ_REPO_GLUE`/`XJ_REPO_AGENTOS`，默认向后兼容本机）——复现机器不再依赖 /vol2 硬编码
3. `deploy.sh`/`status_all.sh`/`start_all.sh`/`iso-sand/start_all.sh`/`stack_ctl.sh` 的 `VERIFY_HOME` 默认优先 `$AGENT_OS_HOME/xuanjian`，旧同构沙盘路径回退（本机运行中实例的 data/ 仍可被监控到）；`env.template` 同步
4. 文档同步：SYSTEM.md 组件表/克隆清单/⑤启动/监督判据、TOPOLOGY.md 表+缺口 #1、本清单
5. **运行时 data/ 不随仓分发**（xuanjian/.gitignore）：daemon.pid/seek/audit/operation_log/信誉积分/模型均为运行产物，新机器首次运行自动创建

**遗留（可选后续）**：本机运行中守护进程仍在旧同构沙盘路径（PID 2033773，未动）；迁移到 xuanjian/ 运行=停旧起新，待 dandan 确认后做。`内核层规范`（PURPOSE.md/snapshots）不在任何仓，复现需自备或置空（守护进程对缺失路径按 FAIL+快照恢复流程处理，不崩溃）。

### #3 edit-web.py 仓分叉 🟠 → ⏳ 待 dandan

**现状**：GitHub main = `3468a4c9`（F180 渲染修复线）；本地 main = `0e11932`，**领先 20 commit**（梦醒阶段2 WAKE_CHANNEL 接线、salience_gate 梦惊讶度第4通道、固定会话 agent:main:轻如烟、wake 通道 A→B、self_pulse 契约修复、dashboard 挂载等），且与远端无共同祖先（`dashboard-mount` 分支含 dashboard 挂载 + 1 个未推 commit）。

**影响**：姐姐 clone edit-web.py main → 编辑器缺唤醒链/梦醒/固定会话修复 → self_pulse 唤醒出口错误、前端消息不刷新、无 dashboard。

**处置**：
- ✅ 本地已修 `sandglass_log_wrapper.py` 硬编码路径（commit `17799d0`，本机行为等价，实测落沙正常）
- ⏳ 推送需合并决策：远端 main 的 F180 修复与本地 20 commit 需 merge（或 force push 丢失 F180），**待 dandan 确认**。dashboard 挂载是否给姐姐 = 业务决策，一并确认
- ⏳ 编辑器 `content[:500]` 截断残留（SYSTEM.md 坑 6 已改描述），建议合并后顺手修

### #4 glue-memory-injector 分叉 🟠 → ✅ 已修

**现状**：远端 main 有 README init + 平行"回魂仪式注入"实现（无共同祖先）；本地生产态含 召回L1-b/阶段2 storeTurn（config 优先 + env fallback 定案）。

**处置（已做）**：`--allow-unrelated-histories -X ours` 合并（代码以本地生产态为准，README 并入）+ 补 .gitignore（*.bak-*）+ 推送。GitHub main = `81e5257`（2026-08-12 16:45 验证）。`node --check` 四文件全过。

### #5 编辑器 wrapper 硬编码路径 🔴 → ✅ 已修（未推）

**现状**：`sandglass_log_wrapper.py` 硬编码 `sys.path.insert(0, '/vol2/1000/AI专用/.../sandglass_source')` + `NEXSANDBASE_HOME=...`。姐姐机器无此路径 → import 失败 → **每轮落沙静默失败（明线断=失忆根源）**；且 edit-web.py `_sandglass_log` 用 `_sp.Popen(..., stdout=DEVNULL, stderr=DEVNULL)` 吞掉错误，无任何告警。

**处置（已做）**：改 `LIGHT_HOME` env 优先 + 脚本位置相对推导（`<LIGHT_HOME>/scripts` 上上级），两种方式本机等价（已实测落沙）。commit `17799d0`（edit-web 仓，随 #3 合并后推送）。

### #6 env.template 缺唤醒/塑形参数 🟡 → ✅ 已修

**现状**：env.template D 节只有 LMS_EMBEDDER/DREAM_*；`WAKE_CHANNEL`/`SG_DREAM_FEED`/`SG_DOUBT_FEED`/`LMS_FEED_*` 缺失 → 姐姐 env.local 缺省 → 唤醒出口默认旧通道 a、塑形开关参数缺省。

**处置（已做）**：env.template D 节补齐（默认值与本机一致；WAKE_CHANNEL=b 按 dandan 8/11 拍板）。

### #7 LMS .env.example 缺键 🟡 → ⏳ 待补（LMS 仓）

**现状**：example 26 键 vs 本机 .env 18 键；example 缺 `LMS_CLOUD_EMBED_FALLBACK_URL`（embed 备用端点）、`LMS_CONTROL_TOKEN`（:8191 控制口）、`DREAM_IDLE_THRESHOLD`/`DREAM_STEPS`/`DREAM_CHECK_INTERVAL`（梦参数）、`LMS_SNAPSHOT_DIR`。

**影响**：姐姐控制口 token 缺省生成、梦参数走默认。低危但建议补全。**处置**：下次 LMS 仓改动时随带（小改动，无需专门 commit）。

### #8 文档本机化/旧入口 🟡 → ✅ 已修

**已改（agent-os，commit `一键启动-*`）**：
- SYSTEM.md：§3 顶部加 deploy.sh 总控说明 + 分层关系；§3.4 加 `deploy.sh cron-show` 模板提示；组件表 #1 更正"找回自己副本已过期"；外部依赖表 embed 行补 `ollama pull bge-m3` 指引；坑 6 更正 500 截断现状；§3.2/§6 巡检表入口改 deploy.sh
- DEPLOY-GLOBAL.md：§2 改为 deploy.sh 推荐 + 手工分步对照 + cron-show 模板
- TOPOLOGY.md：§6 缺口清单整体重写（玄鉴/edit-web/nyx/插件仓现状）

### #9 qingruyan-scripts 重叠 🟡 → ⏳ 待 dandan

**现状**：`github.com/tdx1146/qingruyan-scripts` main 存在；但 self_pulse 全套脚本实际在 edit-web.py 仓（轻如烟/scripts，129 文件已跟踪）。两仓内容重叠，TOPOLOGY §5 仓库全景表需同步。

**建议**：将 qingruyan-scripts 标注废弃（Archived）或合并入 edit-web.py 仓。

### #10 dashboard.html 生成物入库 🟢 → ⏳ 建议

`gen_dashboard.sh` 每 5min 重写 dashboard.html（带时间戳），导致 agent-os 工作树持续脏（当前 `M dashboard.html`）。建议 .gitignore + `git rm --cached`（需申报，属改现有）。

### #11 三入口并存 🟢 → ✅ 已修（文档）

deploy.sh（总控/体检）→ stack_ctl.sh（6 服务表驱动管理）→ start_all.sh（旧雏形，@reboot 兼容）。分层关系已写入 SYSTEM.md §3 / DEPLOY-GLOBAL §2。

---

## 复现路径（姐姐侧，修完后应该这样跑）

```bash
# 1. clone（全部公开，无需 token）
git clone https://github.com/tdx1146/agent-os.git
git clone https://github.com/tdx1146/living-memory-system.git        # 放到 LMS_HOME
git clone https://github.com/tdx1146/memory-integration-layer.git    # 放到 GLUE_HOME
git clone https://github.com/tdx1146/nyx.git                          # 放到 LIGHT_HOME/sandglass_source
git clone https://github.com/tdx1146/edit-web.py.git                  # 放到 LIGHT_HOME/scripts（⚠️ 待 #3 合并后才有生产版）
# 玄鉴：已内置 agent-os/xuanjian/（无需单独 clone；data/ 首次运行自动创建）

# 2. 外部依赖自备
#    embed: 任意机器 ollama pull bge-m3 && ollama serve（端口 11435）→ 写 LMS .env + env.local
#    DeepSeek key: 写 LMS .env
#    OpenClaw: 官方安装 + 插件 glue-memory-injector + MCP lms-memory/lms-http/shouji-memory

# 3. 一键部署
cd agent-os && ./stack_ctl.sh setup    # 生成 env.local，改 A 节 8 路径
bash deploy.sh                          # 前置检测→拉起→健康→cron 检查
bash deploy.sh status                   # 确认 6 服务 + 编辑器 + OpenClaw + cron 全绿
```

## 附：数据边界（重要）

**个人记忆数据（sandglass.txt / snapshots/ / workspace/memory/ 等）不随任何仓库分发**。复现 = 代码复现，新机器从空数据开始。nyx PATCH-README 已加声明；请勿把数据目录提交进仓库（防隐私泄露）。玄鉴运行时 data/（daemon.pid/seek/audit/operation_log）同样不随 agent-os 分发（xuanjian/.gitignore）。
